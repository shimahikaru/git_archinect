<article>
<%= simple_format(comment.text) %>
<div class="title">
<h5><%= link_to comment.user.name, user_path(comment.user) %></h5>
<p><%= comment.created_at.strftime('%Y-%m-%d %H:%M') %></p></div>
</article>
