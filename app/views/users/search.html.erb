<%= stylesheet_link_tag 'user/index', media: 'all', 'data-turbolinks-track': 'reload' %>
<%= stylesheet_link_tag    'products', media: 'all', 'data-turbolinks-track': 'reload' %>
    <% if @users.length == 0 %>
     <div class="search_no">
        <% if user_signed_in? %>
        <p class="product_search">見つかりませんでした。<br ></p>
        <% else %>
        <p class="product_search">見つかりませんでした。<br >是非最初の登録者になりましょう。</p>
            <%= link_to(new_user_registration_path, :class=>"buttonA" ) do %>無料で会員登録<% end %><br >
            <p>または</p><br >
            <%= link_to(new_user_session_path, :class=>"buttonA buttonB" ) do %>ログイン<% end %>
        <% end %>
      </div>
    <% end %>
<div class="user_index">
<%= render partial: 'users/user', collection: @users %>
</div>
<div class="paginate">
<%= paginate @users %>
</div>
